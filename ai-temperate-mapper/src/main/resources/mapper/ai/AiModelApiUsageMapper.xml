<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.temperate.mapper.ai.AiModelApiUsageMapper">

    <resultMap id="UsageResultMap"
               type="com.example.temperate.model.ai.entity.AiModelApiUsage">
        <id property="id" column="id"/>
        <result property="keyDigest" column="key_digest"/>
        <result property="aiModelId" column="ai_model_id"/>
        <result property="billingStatus" column="billing_status"/>
        <result property="promptTokens" column="prompt_tokens"/>
        <result property="completionTokens" column="completion_tokens"/>
        <result property="cachedPromptTokens" column="cached_prompt_tokens"/>
        <result property="chargedQuotaMinor" column="charged_quota_minor"/>
        <result property="finishReason" column="finish_reason"/>
        <result property="failureCode" column="failure_code"/>
        <result property="createdAt" column="created_at"/>
        <result property="settledAt" column="settled_at"/>
    </resultMap>

    <resultMap id="UsageRefundCandidateResultMap"
               type="com.example.temperate.model.ai.entity.AiModelApiUsageRefundCandidate">
        <id property="usageId" column="usage_id"/>
        <result property="loginIdentityId" column="login_identity_id"/>
        <result property="reservedQuotaMinor" column="reserved_quota_minor"/>
    </resultMap>

    <sql id="UsageColumns">
        id,
        key_digest,
        ai_model_id,
        billing_status,
        prompt_tokens,
        completion_tokens,
        cached_prompt_tokens,
        charged_quota_minor,
        finish_reason,
        failure_code,
        created_at,
        settled_at
    </sql>

    <insert id="insert"
            parameterType="com.example.temperate.model.ai.entity.AiModelApiUsage"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO ai_model_api_usage (
            key_digest,
            ai_model_id,
            billing_status
        )
        VALUES (
            #{keyDigest,jdbcType=BINARY},
            #{aiModelId,jdbcType=BIGINT},
            #{billingStatus,jdbcType=SMALLINT}
        )
    </insert>

    <select id="findByIdForUpdate" resultMap="UsageResultMap">
        SELECT <include refid="UsageColumns"/>
        FROM ai_model_api_usage
        WHERE id = #{usageId,jdbcType=BIGINT}
        FOR UPDATE
    </select>

    <update id="settle">
        UPDATE ai_model_api_usage
        SET billing_status = #{billingStatus,jdbcType=SMALLINT},
            prompt_tokens = #{promptTokens,jdbcType=BIGINT},
            completion_tokens = #{completionTokens,jdbcType=BIGINT},
            cached_prompt_tokens = #{cachedPromptTokens,jdbcType=BIGINT},
            charged_quota_minor = #{chargedQuotaMinor,jdbcType=BIGINT},
            finish_reason = #{finishReason,jdbcType=VARCHAR},
            failure_code = #{failureCode,jdbcType=VARCHAR},
            settled_at = #{settledAt,jdbcType=TIMESTAMP_WITH_TIMEZONE}
        WHERE id = #{usageId,jdbcType=BIGINT}
          AND billing_status = #{expectedBillingStatus,jdbcType=SMALLINT}
    </update>

    <select id="findExpiredReservationsForUpdate"
            resultMap="UsageRefundCandidateResultMap">
        SELECT
            usage.id AS usage_id,
            key_record.login_identity_id,
            detail.reserved_quota_minor
        FROM ai_model_api_usage usage
        JOIN ai_model_api_usage_detail detail ON detail.usage_id = usage.id
        JOIN user_api_key key_record ON key_record.key_digest = usage.key_digest
        WHERE usage.billing_status = #{reservedStatus,jdbcType=SMALLINT}
          AND usage.created_at &lt;= #{createdBefore,jdbcType=TIMESTAMP_WITH_TIMEZONE}
        ORDER BY usage.created_at, usage.id
        LIMIT #{limit,jdbcType=INTEGER}
        FOR UPDATE OF usage SKIP LOCKED
    </select>

    <update id="markRefundedBatch">
        WITH candidate(usage_id) AS (
            VALUES
            <foreach collection="candidates" item="candidate" separator=",">
                (#{candidate.usageId,jdbcType=BIGINT})
            </foreach>
        )
        UPDATE ai_model_api_usage usage
        SET billing_status = #{refundedStatus,jdbcType=SMALLINT},
            charged_quota_minor = 0,
            finish_reason = 'SYSTEM_FAILURE_REFUNDED',
            failure_code = #{failureCode,jdbcType=VARCHAR},
            settled_at = #{settledAt,jdbcType=TIMESTAMP_WITH_TIMEZONE}
        FROM candidate
        WHERE usage.id = candidate.usage_id
          AND usage.billing_status = #{reservedStatus,jdbcType=SMALLINT}
    </update>
</mapper>
