<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example.temperate.functions</groupId>
    <artifactId>xai-video-transfer</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>2.18.3</jackson.version>
        <aliyun.oss.v2.version>0.3.1</aliyun.oss.v2.version>
		<junit.version>5.11.4</junit.version>
        <fc.java.runtime.archive>OpenJDK11U-jre_x64_linux_hotspot_11.0.28_6.tar.gz</fc.java.runtime.archive>
        <fc.java.runtime.url>https://github.com/adoptium/temurin11-binaries/releases/download/jdk-11.0.28%2B6/${fc.java.runtime.archive}</fc.java.runtime.url>
        <fc.java.runtime.sha256>ddbd5d7ef14aa06784fb94d1e0e7177868dfdd0aa216a8a2e654869968ef7392</fc.java.runtime.sha256>
        <fc.deploy.directory>${project.build.directory}/fc-deploy</fc.deploy.directory>
    </properties>

    <dependencies>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>alibabacloud-oss-v2</artifactId>
            <version>${aliyun.oss.v2.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>copy-fc-web-bootstrap</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}</directory>
                                    <includes>
                                        <include>bootstrap</include>
                                    </includes>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.example.temperate.functions.video.XaiVideoTransferWebServer</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>assemble-fc-web-deployment</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <delete dir="${fc.deploy.directory}"/>
                                <mkdir dir="${fc.deploy.directory}"/>
                                <get src="${fc.java.runtime.url}"
                                     dest="${project.build.directory}/${fc.java.runtime.archive}"
                                     skipexisting="true"
                                     retries="3"/>
                                <checksum file="${project.build.directory}/${fc.java.runtime.archive}"
                                          algorithm="SHA-256"
                                          property="fc.java.runtime.sha256.actual"/>
                                <condition property="fc.java.runtime.sha256.valid">
                                    <equals arg1="${fc.java.runtime.sha256.actual}"
                                            arg2="${fc.java.runtime.sha256}"/>
                                </condition>
                                <fail unless="fc.java.runtime.sha256.valid"
                                      message="Downloaded Linux Java 11 runtime failed SHA-256 verification."/>
                                <copy file="${project.basedir}/bootstrap"
                                      todir="${fc.deploy.directory}"/>
                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      todir="${fc.deploy.directory}"/>
                                <copy file="${project.build.directory}/${fc.java.runtime.archive}"
                                      todir="${fc.deploy.directory}"/>
                                <zip destfile="${project.build.directory}/xai-video-transfer-deploy.zip"
                                     basedir="${fc.deploy.directory}"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.5.2</version>
			</plugin>
        </plugins>
    </build>
</project>
